name: تست سریع توکن HF

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: تست توکن
        run: |
          TOKEN="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"
          echo "در حال تست..."
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
            -H "Authorization: Bearer $TOKEN" \
            "https://huggingface.co/api/whoami")
          echo "کد وضعیت: $STATUS"
          if [ "$STATUS" = "200" ]; then
            echo "توکن معتبره!"
          else
            echo "توکن نامعتبره یا دامنه بسته‌ست"
          fi
